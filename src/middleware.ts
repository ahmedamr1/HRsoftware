// Simulated Auth Middleware - Pass through all requests
// Role-based protection is handled at the component/page level or via AuthContext

export default function middleware() {
    return;
}

export const config = {
    matcher: [
        '/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)',
        '/(api|trpc)(.*)',
    ],
};
